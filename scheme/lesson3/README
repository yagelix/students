1. Продолжаем изучение замыканий на примере построения генераторов огибающей и осцилляторов. Вложенные замыкания и каррирование многопараметрических функций (приведение к функциям с малым числом аргументов) -- instrument.scm

2. Архитектура мызыкального "ассемблера" (события, генераторы, каналы, потоки и оркестрирование) -- musasm.scm

3. "Объектно-ориентированный" код на замыканиях и лямбдах -- пример канала (channel) и генератора (generator) -- changen.scm

4. Изучаем сложную рекурсию -- map, fold/reduce -- сбор и суммрование значений, групповые операции.

5. Группировка сортированных значений (рекурсивный метод).

6. Тестовый код на ассемблере.
